{{!-- ---
behavior: generated
target: {{outputDir}}
name: {{tagPrefix}}.types.ts
iterate: tags
--- --}}
// AUTO-GENERATED BY specshot (custom template — plain TS types, no Zod)

{{#if imports.length}}
import type { {{#each imports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from "{{{modelsModulePath}}}";
{{/if}}

{{#if specificSchemas.length}}
// -- Specific Types for {{tag}} --
{{#each specificSchemas}}
export type {{name}} = {{{tsType}}};

{{/each}}
{{/if}}

// -- Request & Response Types --
{{#each operations}}
{{#if hasBody}}
export type {{typeNamePayload}} = {{{bodyType}}};
{{/if}}

{{#if hasQuery}}
export interface {{typeNameParams}} {
{{#each queryParams}}
  {{name}}{{#unless required}}?{{/unless}}: {{{tsType}}};
{{/each}}
}
{{/if}}

export type {{typeNameResponse}} = {{{resType}}};

{{/each}}

// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom types here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
