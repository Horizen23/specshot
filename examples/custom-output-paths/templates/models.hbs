// AUTO-GENERATED BY specshot (custom template — plain TypeScript types, no Zod) - DO NOT DELETE MAGIC COMMENTS
// No runtime validation — just plain TypeScript types.

{{#each schemas}}
/**
 * {{name}}
 */
export type {{name}} = {{{tsType}}};

{{/each}}
// ─────────────────────────────────────────────
// Shared Error class
// ─────────────────────────────────────────────
export class ApiError extends Error {
  constructor(
    message: string,
    public readonly status: number,
    public readonly body: string,
  ) {
    super(message);
    this.name = "ApiError";
  }
}

// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom types here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
