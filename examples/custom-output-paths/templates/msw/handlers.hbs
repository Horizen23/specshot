// AUTO-GENERATED BY specshot (custom template — flat MSW handlers, no Zod)
import { http, HttpResponse, type PathParams, delay } from "msw";
{{#if typeImports.length}}
import type { {{#each typeImports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from "{{{typesImportPath}}}";
{{/if}}

export const {{tagLowerCase}}Handlers = [
  {{#each handlers}}
  /**
   * {{summary}}
   */
  http.{{httpMethod}}(
    `{{#if ../baseUrl}}{{../baseUrl}}{{/if}}{{{pathPattern}}}`,
    async ({ params{{#if hasBody}}, request{{/if}} }) => {
      {{#if hasError}}
      return HttpResponse.json(
        {{{errorBody}}},
        { status: {{errorStatus}} }
      );
      {{/if}}
      {{#if hasBody}}
      const body = await request.json() as {{bodyTypeName}};
      {{/if}}
      {{#if delayMs}}
      await delay({{delayMs}});
      {{/if}}
      return HttpResponse.json(
        {{#if customMockData}}{{{customMockData}}}{{else}}{{{mockResponse}}}{{/if}},
        { status: {{statusCode}} }
      );
    }
  ){{#unless @last}},{{/unless}}
  {{/each}}
];
