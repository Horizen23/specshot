// AUTO-GENERATED BY specshot (custom template — schema registry) - DO NOT DELETE MAGIC COMMENTS
// This template exports Zod schemas with the `{{name}}Schema` naming convention
// and infers TypeScript types from them. Schemas are grouped in a registry object
// for convenient runtime access (e.g. `schemas.Pet.parse(input)`).

import { z } from "zod";

{{#each schemas}}
/**
 * {{name}} — Zod schema (see {{name}} type below)
 */
export const {{name}}Schema = {{{zod}}};

/** {{name}} — TypeScript type inferred from {{name}}Schema */
export type {{name}} = z.infer<typeof {{name}}Schema>;

{{/each}}
/**
 * Centralized schema registry.
 * Use for runtime validation: `schemas.Pet.parse(input)`
 */
export const schemas = {
{{#each schemas}}
  {{name}}: {{name}}Schema,
{{/each}}
} as const;

// ─────────────────────────────────────────────
// Shared Error class
// ─────────────────────────────────────────────
export class ApiError extends Error {
  constructor(
    message: string,
    public readonly status: number,
    public readonly body: string,
  ) {
    super(message);
    this.name = "ApiError";
  }
}

// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom schemas here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
