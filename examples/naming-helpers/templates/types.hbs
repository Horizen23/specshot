// AUTO-GENERATED BY specshot (custom template — naming helpers demo)
// Shows naming helpers in action for each operation.

{{#if imports.length}}
import type { {{#each imports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from "{{{modelsModulePath}}}";
{{/if}}

{{#if specificSchemas.length}}
{{#each specificSchemas}}
// {{pascalCase name}} — PascalCase, {{camelCase name}} — camelCase, {{kebabCase name}} — kebab, {{snakeCase name}} — snake
export type {{name}} = {{{tsType}}};

{{/each}}
{{/if}}

{{#each operations}}
// Operation: {{operationId}}
//   pascalCase: {{pascalCase operationId}}
//   camelCase:  {{camelCase operationId}}
//   kebabCase:  {{kebabCase operationId}}
//   snakeCase:  {{snakeCase operationId}}
//   toUpperCase: {{toUpperCase operationId}}
//   toLowerCase: {{toLowerCase operationId}}
{{#if hasBody}}
export type {{typeNamePayload}} = {{{bodyType}}};
{{/if}}

{{#if hasQuery}}
export interface {{typeNameParams}} {
{{#each queryParams}}
  {{name}}{{#unless required}}?{{/unless}}: {{{tsType}}};
{{/each}}
}
{{/if}}

export type {{typeNameResponse}} = {{{resType}}};

{{/each}}

// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom types here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
