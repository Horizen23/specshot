// AUTO-GENERATED BY specshot (custom template — naming helpers demo)
// Demonstrates all available Handlebars naming helpers.
//
// Available helpers:
//   {{capitalize tag}}        → Pets
//   {{camelCase operationId}} → listPets
//   {{pascalCase tag}}        → Pets
//   {{kebabCase tag}}         → pets
//   {{snakeCase tag}}         → pets
//   {{toLowerCase tag}}       → pets
//   {{toUpperCase tag}}       → PETS
//   {{#ifEq a b}}...{{/ifEq}}
//   {{#ifNeq a b}}...{{/ifNeq}}

{{#each schemas}}
// {{pascalCase name}} → PascalCase: {{pascalCase name}}, camelCase: {{camelCase name}}, kebab: {{kebabCase name}}, snake: {{snakeCase name}}, UPPER: {{toUpperCase name}}
export type {{name}} = {{{tsType}}};

{{/each}}

export class ApiError extends Error {
  constructor(
    message: string,
    public readonly status: number,
    public readonly body: string,
  ) {
    super(message);
    this.name = "ApiError";
  }
}

// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom types here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
