{
  "name": "specshot-petstore",
  "private": true,
  "scripts": {
    "mock": "specshot mock --proxy http://localhost:3000 --web",
    "generate": "specshot generate",
    "init": "specshot init"
  },
  "dependencies": {
    "specshot": "file:../.."
  }
}
