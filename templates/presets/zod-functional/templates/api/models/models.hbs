{{!-- ---
behavior: generated
target: {{outputDir}}
name: models.ts
--- --}}
// AUTO-GENERATED BY specshot (zod-functional preset) - DO NOT DELETE MAGIC COMMENTS
// Zod schemas + inferred types. No class system — validation done via standalone functions.

import { z } from "zod";

{{#each schemas}}
export const {{name}}Schema = {{{zod}}};
export type {{name}} = z.infer<typeof {{name}}Schema>;

{{/each}}
/**
 * Centralized schema registry.
 * Use for runtime validation: `schemas.Pet.parse(input)`
 */
export const schemas = {
{{#each schemas}}
  {{name}}: {{name}}Schema,
{{/each}}
} as const;

// ─────────────────────────────────────────────
// Shared Error class
// ─────────────────────────────────────────────
export class ApiError extends Error {
  constructor(
    message: string,
    public readonly status: number,
    public readonly body: string,
  ) {
    super(message);
    this.name = "ApiError";
  }
}

// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom schemas here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
