// AUTO-GENERATED BY specshot (zod-functional preset)
// Zod schemas + plain TypeScript types. Uses interface for query params.

import { z } from "zod";

{{#if imports.length}}
import type { {{#each imports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from "./models";
import { {{#each imports}}{{this}}Schema{{#unless @last}}, {{/unless}}{{/each}} } from "./models";

{{/if}}
{{#if specificSchemas.length}}
// ── Tag-specific schemas for {{tag}} ──
{{#each specificSchemas}}
export const {{name}}Schema = {{{zod}}};
export type {{name}} = z.infer<typeof {{name}}Schema>;

{{/each}}
{{/if}}
// ── Request & Response Types ──
{{#each operations}}
{{#if hasBody}}
/** Request body for {{operationId}} */
export type {{typeNamePayload}} = {{{bodyType}}};

{{/if}}
{{#if hasQuery}}
/** Query parameters for {{operationId}} */
export interface {{typeNameParams}} {
{{#each queryParams}}
  {{name}}{{#unless required}}?{{/unless}}: {{{tsType}}};
{{/each}}
}

{{/if}}
/** Response type for {{operationId}} */
export type {{typeNameResponse}} = {{{resType}}};

{{/each}}
// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom types here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
