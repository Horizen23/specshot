{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Validation Mode",
  "type": "object",
  "properties": {
    "validation": {
      "type": "string",
      "enum": ["types-only", "zod-schemas", "zod-runtime"],
      "default": "zod-schemas",
      "description": "Validation strictness mode"
    }
  }
}
