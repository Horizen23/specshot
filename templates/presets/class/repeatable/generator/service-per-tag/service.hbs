// AUTO-GENERATED BY specshot - DO NOT DELETE MAGIC COMMENTS
import { BaseService } from "{{#if importAlias}}{{importAlias}}/core/base-service{{else}}{{coreRelPath}}/base-service{{/if}}";
import { ApiClient } from "{{#if importAlias}}{{importAlias}}/core/api-client{{else}}{{coreRelPath}}/api-client{{/if}}";
import { ApiResult } from "{{#if importAlias}}{{importAlias}}/core/types{{else}}{{coreRelPath}}/types{{/if}}";
import { AppRequestConfig, AppApiErrorData } from "{{#if importAlias}}{{importAlias}}/types{{else}}{{typesRelPath}}{{/if}}";

{{#if exportsToReExport.length}}
import type { {{#each exportsToReExport}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from "./{{tagPrefix}}.types";

export type { {{#each exportsToReExport}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} };
{{/if}}

export class {{className}} extends BaseService<"{{tagPrefix}}"> {
  constructor(client: ApiClient) {
    super(client, "{{tagPrefix}}");
  }

{{#each operations}}
  /**
   * {{operationId}}{{#if summary}}
   * {{summary}}{{/if}}{{#if description}}
   * {{description}}{{/if}}
{{#each pathParams}}
   * @param {{safe}} - Path parameter
{{/each}}{{#if hasBody}}
   * @param payload - Request body
{{/if}}
   * @param config - Request configuration (headers, timeout, signal, etc.)
   * @returns `{ data, error, ok }`
   *   - `data`: {{#if isDelete}}void{{else}}`{{typeNameResponse}}`{{/if}} (null on error)
   *   - `error`: `ApiError<AppApiErrorData>` | `ClientError` (null on success)
   *     Both have `.message`. Use `error.status` to check for HTTP errors,
   *     or `error.kind` for network/timeout/abort/parse errors.
   *   - `ok`: `true` on success, `false` on error
   *
   * @example
   * const { data, error, ok } = await api.{{../tagPrefix}}.{{methodName}}(...);
   * if (!ok) {
   *   console.error(error.message);
   *   return;
   * }
   * // use `data` safely here
   */
  public {{methodName}}(
{{#each pathParams}}
    {{safe}}: string | number,
{{/each}}
{{#if hasBody}}
    payload: {{typeNamePayload}},
{{/if}}
    config?: {{{configType}}}
  ): Promise<ApiResult<{{#if isDelete}}void{{else}}{{typeNameResponse}}{{/if}}, AppApiErrorData>> {
    return this.client.{{methodLower}}<{{#if isDelete}}AppApiErrorData{{else}}{{typeNameResponse}}, AppApiErrorData{{/if}}>(
      `{{{urlStr}}}`,
{{#if hasBody}}
      payload,
{{/if}}
      this.withSignal(config)
    );
  }

{{/each}}

  // --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
  // Add your custom methods here. Do not remove these comments.
{{/if}}
  // --- CUSTOM CODE END ---
}
