{{!-- ---
behavior: generated
target: {{outputDir}}/..
name: config.ts
--- --}}
// AUTO-GENERATED BY specshot (functional preset) - DO NOT DELETE MAGIC COMMENTS

// ─────────────────────────────────────────────
// Shared Base URL configuration
// ─────────────────────────────────────────────
let _baseUrl = "";

/** Set the global API base URL (e.g. "https://api.example.com"). */
export function setBaseUrl(url: string): void {
  _baseUrl = url.replace(/\/+$/, "");
}

/** Get the global API base URL. */
export function getBaseUrl(): string {
  return _baseUrl;
}

// --- CUSTOM CODE START ---
{{#if customCode}}
{{{customCode}}}
{{else}}
// Add your custom configuration here. Do not remove these comments.
{{/if}}
// --- CUSTOM CODE END ---
