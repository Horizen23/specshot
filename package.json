{
  "name": "specshot",
  "version": "2.0.1",
  "description": "Fire an OpenAPI spec, get strictly-typed TypeScript code — zero-dependency, Zod-validated, shadcn-style injection",
  "type": "module",
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "exports": {
    ".": {
      "types": "./dist/cli.d.ts",
      "import": "./dist/cli.js"
    },
    "./package.json": "./package.json"
  },
  "bin": {
    "specshot": "dist/cli.js"
  },
  "files": [
    "dist",
    "templates",
    "ui/mock-ui/dist"
  ],
  "scripts": {
    "build:ui": "npm --prefix ui/mock-ui install && npm --prefix ui/mock-ui run build",
    "build:cli": "tsup",
    "build": "npm run build:ui && npm run build:cli",
    "dev": "tsup --watch",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "npm run lint:types && npm run lint:format",
    "lint:types": "tsc --noEmit",
    "lint:format": "prettier --check .",
    "format": "prettier --write .",
    "clean": "rm -rf dist ui/mock-ui/dist",
    "prepublishOnly": "npm run build",
    "prepare": "husky"
  },
  "keywords": [
    "api-client",
    "openapi",
    "swagger",
    "typescript",
    "code-generator",
    "codegen",
    "zod",
    "swr",
    "fetch",
    "cli",
    "shadcn",
    "mock",
    "mock-server",
    "api-mocking"
  ],
  "author": "SpecShot Contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Horizen23/specshot.git"
  },
  "bugs": {
    "url": "https://github.com/Horizen23/specshot/issues"
  },
  "homepage": "https://github.com/Horizen23/specshot#readme",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@faker-js/faker": "^10.5.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "figlet": "^1.11.0",
    "handlebars": "^4.7.9",
    "inquirer": "^9.2.15",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/figlet": "^1.7.0",
    "@types/inquirer": "^9.0.9",
    "@types/node": "^25.9.2",
    "husky": "^9.1.7",
    "prettier": "^3.9.1",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^1.6.0"
  }
}
